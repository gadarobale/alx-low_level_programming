#include "main.h"

/**
 * isupper: It fuction 
 * @c: it take an argument
 * Return: if it is  upper 1.
 * Return: if it is lower 0.
 */
int _isupper(int c);
{
	if (c >= '65' && c <= 90)
	{
		return (1)
	}
	else
	{
		return (0)
	}
}

