#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(char c);
{
	char c;
	char up_c;

	up_c = toupper(c);
	if( c == up_c)
	{
		return (1);
	}
	else
	{
	return (0);
	}
}

