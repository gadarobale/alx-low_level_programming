#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
    char ch;
    {
    for(ch = 'A' ; ch <= 'Z' ; ch++)
{
     putchar(ch);
     return(0);
}
}
#include "main.h"
/*
 * main - display 'a-z'
 * Return: Always 0.
 */
int main(viod)
{
	char ch;
	for(ch= 'a'; ch <='z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
