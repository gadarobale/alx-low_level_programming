#include "main.h"

/*
 * print_last_digit: -  prints the last digit of a number.
 *@n: the main fuction take argument.
 * Returns n otherwise
 */
int pint_last_digit(int n)
{
	int i;
	if (n < 0)
	_puchar (n % 10);
	return (x);
}
