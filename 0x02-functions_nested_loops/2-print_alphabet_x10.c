#include "main.h"
/*
 * main - display 'a-z'
 * Return: Always 0.
 */
int main(viod)
{
	char ch;
	for(int i = 0 ; i < 10 ; i++)
	{
		m
        	for(ch = 'a'; ch <= 'z'; ch++)
        	{
		putchar(ch);
		}
	}
      	return (0);
}
